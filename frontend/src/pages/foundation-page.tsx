import { ArrowUpRight, Palette, PanelsTopLeft, Sparkles } from 'lucide-react'
import { Card, EmptyState } from '@/components/ui'

const principles = [{ icon: Palette, title: 'Token-driven theming', text: 'Light, dark, and system themes use semantic CSS variables.' }, { icon: PanelsTopLeft, title: 'Reusable application shell', text: 'Header, navigation, overlays, and footer are shared by every module.' }, { icon: Sparkles, title: 'Human-centered AI', text: 'Clear states and evidence-ready UI patterns keep AI outputs understandable.' }]
export function FoundationPage() { return <div className="space-y-8"><div><p className="mb-2 text-sm font-semibold text-brand">CLASSROOMIQ FOUNDATION</p><div className="flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-3xl font-bold tracking-tight sm:text-4xl">Your teaching intelligence workspace</h1><p className="mt-2 max-w-2xl text-muted">A scalable, responsive foundation for curriculum, lecture, and evidence-driven teaching experiences.</p></div><button className="inline-flex min-h-10 items-center gap-2 rounded-xl bg-brand px-4 text-sm font-semibold text-white">Explore modules <ArrowUpRight size={17}/></button></div></div><nav aria-label="Breadcrumb" className="text-sm text-muted">Workspace <span className="px-2">/</span> Foundation</nav><div className="grid gap-4 md:grid-cols-3">{principles.map(({ icon: Icon, title, text }) => <Card key={title} className="transition hover:-translate-y-0.5 hover:shadow-float"><Icon className="text-brand"/><h2 className="mt-5 font-semibold">{title}</h2><p className="mt-2 text-sm leading-6 text-muted">{text}</p></Card>)}</div><EmptyState title="No workspace data yet" description="Connect the API and start with a curriculum upload. Future feature pages will reuse this layout and design system." /></div> }
